import { CurriculumMap } from "@/components/game/CurriculumMap";

export const metadata = {
  title: "Müfredat Haritası — Vim Ustası",
};

export default function LearnPage() {
  return <CurriculumMap />;
}
